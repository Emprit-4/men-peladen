const configs = {
    folder: "logs",
    fileNames: {
        HTTPReqLog: "request-all.log",
        ServerLog: "process-all.log",
        DBLog: "database-all.log",
    },
};

export default configs;
